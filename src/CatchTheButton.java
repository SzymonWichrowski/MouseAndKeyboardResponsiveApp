import javax.swing.*;
import java.awt.*;

public class CatchTheButton extends JFrame {

    static int frameWidth = 1200, frameHeight = 800;
    public CatchTheButton(){

        setSize(frameWidth, frameHeight);
        setTitle("Catch the button!");
        setLayout(null);
        getContentPane().setBackground(Color.GRAY);
    }
}
